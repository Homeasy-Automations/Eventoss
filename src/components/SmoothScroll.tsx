"use client";
import { useEffect } from "react";
export default function SmoothScroll() {
  useEffect(() => {
    // subtle lenis-like could be added, but keep native for now
  }, []);
  return null;
}
